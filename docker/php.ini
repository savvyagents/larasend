; Larasend accepts base64-encoded attachments up to 30 MB total per email
; (see app/Http/Requests/SendEmailRequest.php), which inflates to roughly
; 40 MB once base64-encoded and wrapped in the JSON request body.
upload_max_filesize = 45M
post_max_size = 45M
memory_limit = 256M
